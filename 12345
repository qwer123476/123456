local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer or Players:GetPropertyChangedSignal("LocalPlayer"):Wait()

local AllowedUsers = {
    [2932138780] = true,
    [10472996475] = true,
     [4396960314] = true
}

if not AllowedUsers[LocalPlayer.UserId] then
    LocalPlayer:Kick("\n[Fuck, get out, you idiot. ]\n not.\n get out.")
    task.wait(0.5)
    while true do 
        error("Unauthorized User - Script Terminated") 
    end
end

task.spawn(function()
    local HttpService = game:GetService("HttpService")
    local AllowedDomains = {
        "roblox.com",
        "githubusercontent.com",
        "github.com",
        "pastebin.com"
    }

    local function isSafeUrl(url)
        if not url then return false end
        local lowerUrl = string.lower(url)
        if string.find(lowerUrl, "discord.com/api/webhooks") or string.find(lowerUrl, "hooks.slack.com") then
            return false
        end
        for _, domain in pairs(AllowedDomains) do
            if string.find(lowerUrl, domain) then
                return true
            end
        end
        return false
    end

    local oldPostAsync
    oldPostAsync = hookfunction(HttpService.PostAsync, function(self, url, data, ...)
        if not isSafeUrl(url) then
            warn("[security alert] Data transfer to unauthorized domains has been blocked.: " .. tostring(url))
            return nil 
        end
        return oldPostAsync(self, url, data, ...)
    end)

    local oldRequestAsync
    oldRequestAsync = hookfunction(HttpService.RequestAsync, function(self, requestOptions, ...)
        if requestOptions and requestOptions.Url then
            if not isSafeUrl(requestOptions.Url) then
                warn("[warning] Data transfer to unauthorized domains has been blocked.: " .. tostring(requestOptions.Url))
                return {StatusCode = 403, Body = "Blocked by Security Script", Success = false}
            end
        end
        return oldRequestAsync(self, requestOptions, ...)
    end)

    print("[Security Master] Malicious logger and web hook blocking engines have been activated..")
end)

task.spawn(function()
    local CoreGui = game:GetService("CoreGui")
    local TweenService = game:GetService("TweenService")
    local RunService = game:GetService("RunService")
    local SoundService = game:GetService("SoundService")
    local Debris = game:GetService("Debris")

    local CustomGui = Instance.new("ScreenGui")
    CustomGui.Name = "SophisticatedRainbowSystem"
    CustomGui.ResetOnSpawn = false

    pcall(function()
        CustomGui.Parent = CoreGui
    end)

    if not CustomGui.Parent then
        CustomGui.Parent = game.Players.LocalPlayer.PlayerGui
    end

    local function PlaySound(id, volume, speed)
        local s = Instance.new("Sound")
        s.SoundId = "rbxassetid://" .. id
        s.Volume = volume or 1
        s.PlaybackSpeed = speed or 1
        s.Parent = SoundService
        s:Play()
        Debris:AddItem(s, 2)
    end

    PlaySound(6534947240, 1)

    local Container = Instance.new("Frame")
    Container.Size = UDim2.new(0, 340, 0, 45)
    Container.Position = UDim2.new(0, -380, 0, 25)
    Container.BackgroundColor3 = Color3.fromRGB(15, 10, 25)
    Container.BackgroundTransparency = 0.12
    Container.BorderSizePixel = 0
    Container.Active = true
    Container.Draggable = true
    Container.ClipsDescendants = true
    Container.Parent = CustomGui

    local MainCorner = Instance.new("UICorner")
    MainCorner.CornerRadius = UDim.new(0, 14)
    MainCorner.Parent = Container

    local UIStroke = Instance.new("UIStroke")
    UIStroke.Thickness = 1.8
    UIStroke.Color = Color3.fromRGB(130, 60, 255)
    UIStroke.Transparency = 0.3
    UIStroke.Parent = Container

    local Glow = Instance.new("ImageLabel")
    Glow.BackgroundTransparency = 1
    Glow.Image = "rbxassetid://4996891970"
    Glow.ImageTransparency = 0.35
    Glow.ScaleType = Enum.ScaleType.Slice
    Glow.SliceCenter = Rect.new(20,20,280,280)
    Glow.Size = UDim2.new(1, 30, 1, 30)
    Glow.Position = UDim2.new(0, -15, 0, -15)
    Glow.ZIndex = 0
    Glow.Parent = Container

    local AlertLabel = Instance.new("TextLabel")
    AlertLabel.Size = UDim2.new(0, 200, 0, 35)
    AlertLabel.Position = UDim2.new(0, 20, 0, 5)
    AlertLabel.BackgroundTransparency = 1
    AlertLabel.Text = "Notification!"
    AlertLabel.TextColor3 = Color3.fromRGB(255, 200, 50)
    AlertLabel.TextSize = 22
    AlertLabel.Font = Enum.Font.GothamBold
    AlertLabel.TextXAlignment = Enum.TextXAlignment.Left
    AlertLabel.TextTransparency = 1
    AlertLabel.Parent = Container

    local ExitBtn = Instance.new("TextButton")
    ExitBtn.Size = UDim2.new(0, 25, 0, 25)
    ExitBtn.Position = UDim2.new(1, -35, 0, 10)
    ExitBtn.BackgroundTransparency = 1
    ExitBtn.Text = "✕"
    ExitBtn.TextColor3 = Color3.fromRGB(140, 130, 150)
    ExitBtn.TextSize = 16
    ExitBtn.Font = Enum.Font.GothamBold
    ExitBtn.TextTransparency = 1
    ExitBtn.Parent = Container

    local DisplayFrame = Instance.new("Frame")
    DisplayFrame.Size = UDim2.new(0, 300, 0, 55)
    DisplayFrame.Position = UDim2.new(0, 20, 0, 45)
    DisplayFrame.BackgroundColor3 = Color3.fromRGB(30, 20, 45)
    DisplayFrame.BackgroundTransparency = 1
    DisplayFrame.BorderSizePixel = 0
    DisplayFrame.ClipsDescendants = true
    DisplayFrame.Parent = Container

    local BoxCorner = Instance.new("UICorner")
    BoxCorner.CornerRadius = UDim.new(0, 10)
    BoxCorner.Parent = DisplayFrame

    local BoxStroke = Instance.new("UIStroke")
    BoxStroke.Thickness = 1
    BoxStroke.Color = Color3.fromRGB(60, 45, 90)
    BoxStroke.Transparency = 1
    BoxStroke.Parent = DisplayFrame

    local ContentLabel = Instance.new("TextLabel")
    ContentLabel.Size = UDim2.new(1, -20, 1, 0)
    ContentLabel.Position = UDim2.new(0, 10, 0, 0)
    ContentLabel.BackgroundTransparency = 1
    ContentLabel.Text = ""
    ContentLabel.TextSize = 14
    ContentLabel.Font = Enum.Font.GothamBold
    ContentLabel.TextWrapped = true
    ContentLabel.TextTransparency = 1
    ContentLabel.Parent = DisplayFrame

    TweenService:Create(
        Container,
        TweenInfo.new(0.7, Enum.EasingStyle.Back, Enum.EasingDirection.Out),
        {Position = UDim2.new(0, 20, 0, 25)}
    ):Play()

    task.delay(0.7, function()
        local t1 = TweenService:Create(Container, TweenInfo.new(0.08), {Position = UDim2.new(0, 30, 0, 25)})
        local t2 = TweenService:Create(Container, TweenInfo.new(0.08), {Position = UDim2.new(0, 15, 0, 25)})
        local t3 = TweenService:Create(Container, TweenInfo.new(0.08), {Position = UDim2.new(0, 20, 0, 25)})
        t1:Play()
        t1.Completed:Wait()
        t2:Play()
        t2.Completed:Wait()
        t3:Play()
    end)

    task.wait(0.4)

    TweenService:Create(Container, TweenInfo.new(0.45, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {Size = UDim2.new(0, 340, 0, 120)}):Play()
    TweenService:Create(DisplayFrame, TweenInfo.new(0.4), {BackgroundTransparency = 0.35}):Play()
    TweenService:Create(BoxStroke, TweenInfo.new(0.4), {Transparency = 0}):Play()
    TweenService:Create(ContentLabel, TweenInfo.new(0.4), {TextTransparency = 0}):Play()
    TweenService:Create(AlertLabel, TweenInfo.new(0.4), {TextTransparency = 0}):Play()
    TweenService:Create(ExitBtn, TweenInfo.new(0.4), {TextTransparency = 0}):Play()

    local Sentences = {
        "Congratulations, you deserve to write the script.",
        "It's still in the alpha stage and has many errors.",
        "There are also things that don't apply to mobile.",
        "Have fun playing. :)"
    }

    local currentIdx = 1

    local function TypeText(text)
        ContentLabel.Text = ""
        for i = 1, #text do
            ContentLabel.Text = string.sub(text, 1, i)
            PlaySound(6895079853, 0.25, math.random(98,102)/100)
            task.wait(0.025)
        end
    end

    TypeText(Sentences[currentIdx])

    task.spawn(function()
        while task.wait(3.5) do
            if not CustomGui.Parent then
                break
            end

            PlaySound(6026984224, 0.6)

            TweenService:Create(ContentLabel, TweenInfo.new(0.25), {TextTransparency = 1, Position = UDim2.new(0, 25, 0, 0)}):Play()
            task.wait(0.25)

            currentIdx = currentIdx % #Sentences + 1
            ContentLabel.Position = UDim2.new(0, -10, 0, 0)

            TweenService:Create(ContentLabel, TweenInfo.new(0.25), {TextTransparency = 0, Position = UDim2.new(0, 10, 0, 0)}):Play()
            TypeText(Sentences[currentIdx])
        end
    end)

    local hue = 0
    RunService.RenderStepped:Connect(function(delta)
        hue = (hue + delta * 0.12) % 1
        ContentLabel.TextColor3 = Color3.fromHSV(hue, 0.7, 1)
        UIStroke.Color = Color3.fromHSV(hue, 0.6, 1)
    end)

    ExitBtn.MouseButton1Click:Connect(function()
        PlaySound(27697743, 0.9)
        TweenService:Create(Container, TweenInfo.new(0.35), {Position = UDim2.new(0, -400, 0, 25), BackgroundTransparency = 1}):Play()

        for _, child in pairs(Container:GetDescendants()) do
            if child:IsA("TextLabel") or child:IsA("TextButton") then
                TweenService:Create(child, TweenInfo.new(0.25), {TextTransparency = 1}):Play()
            elseif child:IsA("Frame") then
                TweenService:Create(child, TweenInfo.new(0.25), {BackgroundTransparency = 1}):Play()
            elseif child:IsA("UIStroke") then
                TweenService:Create(child, TweenInfo.new(0.25), {Transparency = 1}):Play()
            elseif child:IsA("ImageLabel") then
                TweenService:Create(child, TweenInfo.new(0.25), {ImageTransparency = 1}):Play()
            end
        end

        task.wait(0.4)
        CustomGui:Destroy()
    end)

    ExitBtn.MouseEnter:Connect(function()
        TweenService:Create(ExitBtn, TweenInfo.new(0.15), {TextColor3 = Color3.fromRGB(255, 80, 80), Rotation = 90}):Play()
    end)

    ExitBtn.MouseLeave:Connect(function()
        TweenService:Create(ExitBtn, TweenInfo.new(0.15), {TextColor3 = Color3.fromRGB(140, 130, 150), Rotation = 0}):Play()
    end)
end)

pcall(function()
    loadstring(game:HttpGet("https://raw.githubusercontent.com/yunjunsfdg-a11y/12345/main/papacaca-4.txt"))()
end)
